{% extends 'admin.html.twig' %}

{% block page_title %}

    {{ 'crud.title.new'|trans({'%entity_class_name%': 'Parametro'}, 'MicayaelAdminLteMakerBundle') }}

{% endblock %}

{% block breadcrumb %}

    {% embed '@MicayaelAdminLteMaker/Widgets/breadcrumb.html.twig' %}

        {% block content %}
            <li><a href="{{ path('parametro_index') }}">Parametros</a></li>
            <li class="active">{{ 'crud.action.new'|trans({}, 'MicayaelAdminLteMakerBundle') }}</li>
        {% endblock %}

    {% endembed %}

{% endblock %}

{% block page_content %}

    <div class="row">
        <div class="col-md-12">

            {% embed '@MicayaelAdminLteMaker/Widgets/context_menu.html.twig' %}

                {% block actions %}
                    <li>
                        {{ create_link('index', 'parametro_index', {}, 'ROLE_PARAMETRO_READ') }}
                    </li>
                {% endblock %}

            {% endembed %}

        </div>
    </div>

    <div class="row crud-new crud-form">
        <div class="col-md-12">

            {{ form_start(form) }}

                {% embed '@AdminLTE/Widgets/box-widget.html.twig' %}

                    {% block box_body %}
                        {{ form_row(form.dominio) }}
                        {{ form_row(form.tipo) }}
                        {{ form_row(form.codigo) }}
                        {{ form_row(form.valor) }}
                        {{ form_row(form.orden) }}

                    {% endblock %}

                    {% block box_footer %}

                        <div class="mandatory_fields_message">{{ 'crud.msg.mandatory_fields'|trans({}, 'MicayaelAdminLteMakerBundle') }}</div>

                        {{ form_widget(form.saveAndEdit) }}
                        {{ form_widget(form.saveAndList) }}
                        {{ form_widget(form.saveAndAdd) }}

                    {% endblock %}

                {% endembed %}

            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}
